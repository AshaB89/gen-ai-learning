# Shared Utilities

Simple utility functions for AI/ML projects.

## Usage

```python
from shared import clean_text, split_data, normalize_data
```

## Available Functions

- `clean_text()` - Clean and normalize text
- `split_data()` - Split data into train/test sets  
- `normalize_data()` - Normalize numerical data
